import expect from 'expect';
import React from 'react';
import { mount, shallow } from 'enzyme';
import HellTest from '../basicComp';


describe('Component: messageDetail', ()=> {
	it('does all the things 1234',()=>{
		expect(true).toEqual(true);
	});
});